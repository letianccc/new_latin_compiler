

变量与作用域：
创建：
    函数标识符：确认最外层不存在标识符
    局部变量、形参：确认当前作用域不存在标识符
使用：
    函数标识符：确认最外层存在标识符
    局部变量、实参、形参：确认所有作用域存在标识符


无论什么类型的返回值，return的值都会放在%eax中，
比如返回的short值（但返回类型不一定是short）会通过扩展放到%eax，
根据返回值类型不同，用不同方式从%eax中取值。
