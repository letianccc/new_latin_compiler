
实现变量声明

增加变量声明方式
将形参与局部变量的声明合并

实现浮点数

目前汇编输出 $1.5
访问float_symbol需要在使用access_name前把浮点数加载到内存中，需要几行emit_code

分配slot的逻辑应该放到emit外面
str to double  10进制  待实现

emit需要重构  分配栈空间的逻辑需要放到一个地方去

产生浮点数汇编指令的地方需要重构


需要把部分逻辑从emit过程转移到gen过程，即增加中间代码




assert 报错 工具
